import {withRouter} from 'react-router-dom'
import {ProductsConnector} from "./ProductsConnector"
//import PropTypes from "prop-types"

/*
static propTypes = {
    match: PropTypes.object.isRequired,
    location: PropTypes.object.isRequired,
    history: PropTypes.object.isRequired
} */
/*
const { match, location, history} = this.props

const mergeProps = () => ({
    currentPage: Number(this.props.match.params.page),
    navigateToPage: (page) => this.props.history.push(`/shop/products/${this.props.match.params.category}/${page}`)
})

export const ProductPageConnector = () => 
    withRouter(mergeProps())(ProductsConnector)
    */